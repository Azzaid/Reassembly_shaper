import React from 'react';
import PropTypes from 'prop-types';
import styled from "styled-components"

const StyledVerticesInput = styled.div`
`

const VerticesInput = ({vertices, updateShape}) => {
  return (
    <StyledVerticesInput>

    </StyledVerticesInput>
  );
}

VerticesInput.propTypes = {};
VerticesInput.defaultProps = {};

export default VerticesInput;